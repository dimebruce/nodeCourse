//¿Qué es una API
    Es una forma ya revisada o estandarizada de traer información para ser consultada 
    por un cliente. 

        Ejemplo: API de maps de Google. 

// ¿Qué es una API REST
    Es lo mismo que la primera, lo único que cambia es que está ocupa VERBOs HTTP como:
        GET, 
        POST,
        UPDATE, 
        DELETE, 
    Y estásn hechas o creadas para la manipulación de la información, no sólo de consulta. 

// ¿Qué es npm? 
    Es un administrador de paquetes, el cual nos facilita nuestro código, 
    no es más que códigos de terceros empaquetados listos para ser llamados o instalados
    como DEPENDENCIAS de nuestro proyecto. 

// ¿Qué es el package.json? 
    Es el archivo donde se encuentra info relevante de nuestro proyecto como la versión
    nombre, descripción, main, licencia, y sus DEPENDENCIAS. 

    Algo importante es que es un formato de objeto -> Object. 

// Cómo generar mi package.json
    npm init -y

// ¿Cómo instalar un paquete de npm

    npm install nombrePaquete

